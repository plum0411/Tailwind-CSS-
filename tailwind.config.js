/** @type {import('tailwindcss').Config} */
const colors = require('tailwindcss/colors')

module.exports = {
  content: ['./src/**/*.{html,js,vue}'],
  theme: {
    fontFamily: {
    },
    extend: {
      colors: {
      },
      spacing: {
      },
      borderRadius: {
      }
    }
  },
}